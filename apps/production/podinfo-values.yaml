apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: podinfo
spec:
  values:
    ingress:
      hosts:
        - host: podinfo.modularcoding.com
          paths:
            - path: /
              pathType: ImplementationSpecific
