apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: redis
  namespace: redis
spec:
  values:
    usePassword: false
    cluster:
      enabled: false
    master:
      persistence:
        enabled: false
